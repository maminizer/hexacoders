<h1>Creation d'un commentaire </h1>
{% form_theme formCommentaire 'bootstrap_4_layout.html.twig' %}
{{ form_start(formCommentaire) }}

    {{ form_row(formCommentaire.contenu, {'attr' : { 'placeholder' : "contenu de commentaire"} })    }}
    {{ form_row(formCommentaire.produit)   }}


<button type="submit" class="btn btn-danger">
        Enregister
</button>
{{ form_end(formCommentaire) }}