    {% extends 'admin/base.html.twig' %}
{% block body %}
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

                    {{ form_row(form.nom, {'attr' : { 'placeholder' : "Titre de catalogue"} })    }}

                    {{ form_row(form.description, {'attr' : { 'placeholder' : "Contenu de catalogue"} })    }}


                    {{ form_end(form) }}
{% endblock %}